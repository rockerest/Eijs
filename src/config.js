define(
    function(){
        window.ei = window.ei || {};
        window.ei.utility = window.ei.utility || {};

        if( !window.ei.utility.unique ){
            window.ei.utility.unique = 0;
        }
    }
);
